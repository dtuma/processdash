#! /bin/sh

# Runs the IzPack Frontend

# Sets the IzPack home directory
IZPACK_HOME='C:\temp\IzPack'

# Does the effective launching
java -jar "${IZPACK_HOME}/lib/frontend.jar" -HOME "$IZPACK_HOME" "$@"
