   <file targetdir="$INSTALL_PATH" override="true" 
	 src="${pdash.dist.dir}/${teamProcess.zip}"/>

   <file targetdir="$INSTALL_PATH" override="true" 
	 src="${pdash.dist.dir}/${teamProcess2.zip}"/>
